function mySentence(adv, v, n, adj) {
    const wordBlanks = "I " + adv + " " + v + " from the " + n + ", which was disconcertingly " + adj;
    return wordBlanks;
}

return mySentence("quickly", "ran", "dog", "big");